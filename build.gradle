//project.extensions.add("tsung", "/usr/bin/tsung")

repositories {
    mavenCentral()
}

task wrapper(type: Wrapper) {
    gradleVersion = '3.3'
}

task tsung(type: Exec) {
    commandLine "echo", "hello"
}